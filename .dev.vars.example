JWT_SECRET="replace-with-local-development-secret"
FRONTEND_ORIGIN="http://localhost:5173"
PUBLIC_API_ORIGIN="http://127.0.0.1:8787"
ENVIRONMENT="development"
TEMP_EMAIL="church@example.com"
TEMP_PASSWORD="temporary-password"
PLANNING_CENTER_CLIENT_ID=your-id
PLANNING_CENTER_CLIENT_SECRET=your-secret
PLANNING_CENTER_REDIRECT_URI="[baseURL]/api/planning-center/callback"
PLANNING_CENTER_REDIRECT_URI_LOCAL="http://localhost:8787/api/planning-center/callback"
# Optional: password reset emails (without RESEND_API_KEY, reset links are logged in wrangler dev console)
# RESEND_API_KEY="re_..."
# RESEND_FROM="ServeWell <onboarding@resend.dev>"
# Required for schedule publish/update emails in production; optional locally (emails skipped if unset)
# PUBLIC_API_ORIGIN is set above for local resource download links in volunteer emails
# Optional: founder inbox when a new organization registers (requires RESEND_API_KEY to send)
# FOUNDER_NOTIFY_EMAIL="you@example.com"